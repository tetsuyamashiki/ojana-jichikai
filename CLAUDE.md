# CLAUDE.md

このフォルダで作業を始める前に、必ず [PROJECT_NOTES.md](PROJECT_NOTES.md) を読み、
プロジェクトの経緯・ファイル構成・進行中のタスク・作業ログを確認すること。
作業が一区切りしたら、経緯や決定事項を PROJECT_NOTES.md の「作業ログ」に追記して引き継ぎを維持する。

## 作業上のお願い

- git操作(add/commit/push)は基本的にClaudeが直接実行してよい。ただしCoworkモードで作業した場合、
  Coworkのサンドボックスからは認証情報を参照できずpushできないため、commitまでに留め、pushはユーザーが
  Macのターミナルで行うか、後続のCode/CLIセッションに依頼すること
- git操作でGitHubトークンなどの機密情報をチャットやコマンド出力に表示しない(redactして扱う)
- バイナリファイル(画像等)はClaudeが直接書き込めないため、Python(Pillow/pypdfium2)経由で生成・保存する
- 画像追加時は拡張子の大文字小文字に注意する(例: image11.JPG)
- 破壊的なgit操作(force push、reset --hardなど)は行わない
- サイトの大きな更新をpushした後は、Google Search Consoleで該当URLの
  「インデックス登録をリクエスト」を行うようユーザーに案内する(または依頼があれば実施する)
- 行事・イベント欄は自治会だよりの最新号に合わせて更新するのが基本運用。月をまたぐ告知は
  読み手(年配の方が多い)が誤読しないよう、区切りや日付の明記で分かりやすく表示する
